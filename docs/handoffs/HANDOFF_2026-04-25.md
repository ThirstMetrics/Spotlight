---
name: Spotlight handoff 2026-04-25
description: Org-scoping + ItemSetupRequest workflow pushed to origin/main; Mac mini picks up the build.
---

# Handoff — 2026-04-25

Two commits up to `origin/main` since the last Mac Air session. Mac mini takes the build from here. Production runtime is the DigitalOcean droplet at `spotlight.thirstmetrics.com`; droplet pulls from GitHub.

## TL;DR

- `origin/main` HEAD is `8acddc6` — org-scoped queries, RBAC tightened, `ItemSetupRequest` model + migration + admin UI/API.
- Local DB (`localhost:5432/spotlight` via Postgres.app) is already migrated and in sync — `prisma migrate status` is clean.
- **Droplet still needs `prisma migrate deploy` + container rebuild** or `/admin/item-requests` will 500 on missing table.
- `HANDOFF.md` (old 2026-03-23) and `scripts/seed-stations.ts` (1916-line Stations seeder) are deliberately still untracked — separate scope, leave for a future commit.

---

## 1. What landed

### `cfd0fc9` — RBAC enforcement, role banner, RWLV form
Was sitting unpushed on the Mac Air since 2026-03-12. Pushed today.
- Sidebar dedupe for distributor/supplier roles
- Role indicator banner in header (SPOTLIGHT | Org | Role)
- JWT carries distributor/supplier/org names for banner rendering
- RWLV 16-column Item Add Form replaces the generic add form
- Distributors only see their own products on supplier pages
- Map GPS recentered on actual RW building footprint (36.1341)

### `8acddc6` — org-scoped queries + ItemSetupRequest workflow
- **Every query module** now takes optional `organizationId` and scopes accordingly: `overview`, `admin`, `alerts`, `analytics`, `compliance`, `direct`, `distributor-detail`, `inventory`, `map`, `margins`, `messages`, `outlets`, `partners`, `recipes`, `supplier-detail`. Pages thread `organizationId` through from session.
- **`lib/rbac.ts` `filterByScope`** — VP/DIRECTOR no longer get unconditional full access; they're scoped to `organizationId` when present. Fall-through to full access only when `organizationId` is unset (super-admin case).
- **`ItemSetupRequest` model** — distributor-submitted RWLV item proposal with `SetupRequestStatus` enum (`PENDING` / `APPROVED` / `REJECTED` / `NEEDS_INFO`). Relations: `Distributor`, `User` (submitter, reviewer). 16 RWLV form fields stored as strings (matches the form columns rather than parsing into typed fields up front).
- **Migration `20260312205442_add_item_setup_requests`** — creates `item_setup_requests`, the enum, and three FKs (distributor cascade, submitter restrict, reviewer set-null).
- **`/admin/item-requests`** page + `api/admin/item-requests` route — director-side review queue.
- **`auth-initializer.tsx`** — client component that bootstraps auth state on dashboard layout.
- **Sidebar** — adds "Item Requests" under System; renders nothing instead of all-groups when role is unresolved (was leaking nav during JWT load).

53 files, +1206 / −243.

---

## 2. Mac mini bring-up

```
cd ~/Spotlight_Dev
git pull                                              # -> 8acddc6
pnpm install
pnpm --filter @spotlight/db prisma migrate deploy     # apply 20260312205442 to local DB
pnpm --filter @spotlight/db prisma generate
pnpm build
```

`.env` is local-only. Confirm `DATABASE_URL=postgresql://spotlight:spotlight_dev@localhost:5432/spotlight` (or whatever the Mac mini Postgres uses) before any prisma command.

Per memory: check `lsof -i :5432` first — Postgres.app is the local convention, no need to spin up Docker Postgres for dev.

---

## 3. Droplet deploy (when ready)

```
ssh droplet
cd ~/spotlight && git pull
pnpm install --frozen-lockfile
pnpm --filter @spotlight/db prisma migrate deploy     # MUST run before container restart
docker compose build web
docker compose up -d web
```

If `migrate deploy` is skipped, the new `/admin/item-requests` route will 500 — table doesn't exist yet on the droplet's Postgres. Existing queries are backward-compatible (organizationId is optional), so the rest of the app stays up regardless.

---

## 4. State carried forward

- **Stations Casinos seed (`scripts/seed-stations.ts`)** — 1916 lines, additive-only, generates 7 properties. Not yet committed; needs review before going up.
- **Old root-level `HANDOFF.md`** (dated 2026-03-23) — predates the migration. Either fold into `docs/handoffs/` history or delete.
- **GitHub Dependabot** — 33 vulnerabilities flagged on push (18 high, 15 moderate). Worth a triage session.
- **Hammer integration** — companion repo (menu builder, shared schema). Not touched this session; check whether the new `ItemSetupRequest` model is visible from Hammer's Prisma client, since they share `@spotlight/db`.

---

## 5. Repo state

```
repo:    /Users/thirstmetricsm1/Spotlight_Dev
remote:  git@github.com:ThirstMetrics/Spotlight.git
branch:  main
HEAD:    8acddc6 feat: scope queries by organizationId + ItemSetupRequest workflow
status:  clean except HANDOFF.md + scripts/seed-stations.ts (intentionally untracked)
```

---

## 6. Open questions for next session

1. **Should the migration timestamp be regenerated?** The migration is dated `20260312205442` (Mar 12) but only landed on origin today (Apr 25). It applies cleanly because no other migration was added in between, but if any hot-path migrations were created on the droplet's DB out-of-order, ordering could matter.
2. **`organizationId` fallback for true super-admins** — `filterByScope` falls through to full access when `organizationId` is unset. Is "no org" still a valid super-admin shape, or should we now require an explicit `SUPER_ADMIN` role?
3. **`ItemSetupRequest` field types** — all 16 RWLV form fields are stored as strings. If we ever want to query/aggregate (e.g., "how many requests with vendor=X this month"), we'll want to normalize. Worth doing before the table fills up.
